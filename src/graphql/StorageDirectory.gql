query StorageDirectory($path: String!) {
  storageDirectory(path: $path) {
    path
    bucket {
      ...BucketFragment
    }
    folders {
      ...FolderFragment
    }
    files {
      ...FileFragment
    }
  }
}
